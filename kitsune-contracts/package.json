{
	"name": "@kitsune-wallet/contracts",
	"author": "Hadrien Croubois <hadrien.croubois@gmail.com>",
	"version": "0.3.4",
	"license": "MIT",
	"files": [
		"/contracts",
		"/build",
		"/deployments",
		"/test"
	],
	"scripts": {
		"build":      "waffle waffle.json",
		"dry-deploy": "GIT=$(git rev-parse HEAD) DRYRUN=true ts-node deploy.ts",
		"deploy":     "GIT=$(git rev-parse HEAD) ts-node deploy.ts",
		"deploy-all": "yarn deploy ropsten && yarn deploy goerli && yarn deploy kovan",
		"test":       "mocha 'test/**/*.{js,ts}'",
		"lint":       "yarn lint:sol",
		"lint:sol":   "solium -d contracts",
		"clean":      "rm -rf build"
	},
	"dependencies": {
		"openzeppelin-solidity": "^2.2.0",
		"zos-lib": "^2.4.0"
	},
	"devDependencies": {
		"@ensdomains/ens": "^0.3.11",
		"@ensdomains/resolver": "^0.1.6",
		"@types/chai": "^4.1.7",
		"@types/chai-as-promised": "^7.1.0",
		"@types/mocha": "^5.2.6",
		"chai": "^4.2.0",
		"chai-as-promised": "^7.1.1",
		"ens-builder": "^2.0.1",
		"ethereum-waffle": "2.0.10",
		"ethers": "^4.0.27",
		"mocha": "^5.2.0",
		"jq.node": "^2.1.2",
		"mocha": "^6.0.2"
	}
}
